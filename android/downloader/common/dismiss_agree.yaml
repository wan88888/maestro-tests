appId: free.video.downloader.converter.music
---
- runFlow:
    when:
      true: ${DISMISS_AGE}
    commands:
      - tapOn:
          text: ${DISMISS_AGE}
          optional: true
      - tapOn:
          text: ${DISMISS_OK}
          optional: true

- runFlow:
    when:
      true: ${DISMISS_COOKIE}
    commands:
      - tapOn:
          text: ${DISMISS_COOKIE}
          optional: true

- runFlow:
    when:
      true: ${DISMISS_ENTER}
    commands:
      - tapOn:
          text: ${DISMISS_ENTER}
          optional: true
